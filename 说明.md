



普通Vue 项目 和 Lerna 项目 的区别

| 你的操作         | 普通 Vue 项目         | 	Lerna 项目 (这里)     | 	说明                                                                     |
|:-------------|:------------------|:-------------------|:------------------------------------------------------------------------|
| **安装依赖**     | npm install       | npm run bootstrap  | 它会先执行 npm i 安装根目录依赖，再执行 lerna bootstrap 安装所有子包的依赖并链接本地包。这是你最需要记住的第一个命令。 |
| 启动开发服务器      | npm run dev       | npm run dev:ele    | 因为有很多个包，你需要指定为哪个 UI 框架启动开发环境。你需要 cd 到对应的子包目录，或者运行根目录定义好的脚本。             |
| 构建           | npm run build     | npm run build      | 根目录的 build 脚本很可能使用 esno tools/cli/run.ts build 这个自定义脚本来一次性构建所有包。        |
| 清理           | 手动删 node_modules  | npm run clear      | 它定义了复杂的清理脚本，可以一次性清理所有子包的 node_modules 和构建产物。                            |


## 运行

nodejs版本使用16~v17 


```bash

#npm install -g @vue/cli



yarn install

yarn install
npm install @form-create/ant-design-vue@nex

npm run bootstrap

```

### 重新运行

```shell

#nodejs版本使用v20 
# 然后使用 pnpm
npm install -g pnpm

```
> 有些情况出差，尝试重新安装 
```shell
# 1. 清缓存
rm -rf node_modules
rm -rf pnpm-lock.yaml
rm -rf packages/**/node_modules

# 使用过node v20
# 2. 重新安装
yarn install --ignore-engine

# 3. 重新 build
#pnpm run build

# 4. 再启动
NODE_OPTIONS=--openssl-legacy-provider yarn run dev:antdv

# 打包
 yarn run build -p ant-design-vue

# 

```

### 错误后一些修复

```shell
#yarn --ignore-engines add -D -W @vue/babel-plugin-jsx
yarn --ignore-engines  add -D -W \
@rollup/plugin-babel \
@babel/core \
@vue/babel-plugin-jsx

```
或者
```shell
# 先确保引擎检查被忽略（避免 node-ipc 报错）
yarn config set ignore-engines true

# 安装 Vue 3 JSX 所需依赖
yarn add -D -W \
  @rollup/plugin-babel \
  @babel/core \
  @vue/babel-plugin-jsx

# 恢复引擎检查（可选）
yarn config delete ignore-engines

```


## 打包和运行

```shell

# 使用node 
nvm use v20.19.0


pnpm add -g lerna

pnpm install 

# 运行
 pnpm run dev:antd

# 打包
 pnpm run build   
 
 

 
 
```

## 2026-1013 最新的
```shell
# 使用node20 
# 因为 项目中依赖的 @achrinza/node-ipc@9.2.2 这个包明确限制了支持的 Node.js 版本为 8, 10, 12, 14, 16, 17。而你当前系统使用的 Node.js 版本是 20.19.0，超出了它的支持范围。
# 会报错
# 所有 添加 --ignore-engines
yarn install --ignore-engines

# 因为 编译类型兼容性问题，升级了ts
yarn add -D -W typescript@^4.7.0 --ignore-engines

 # 调试 
 yarn run dev:antdv
 # NODE_OPTIONS=--openssl-legacy-provider yarn run dev:antdv
 # 编译并打包
 yarn run build -p ant-design-vue

```
